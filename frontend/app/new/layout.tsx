export const metadata = {
  title: "New Project - Innovation Workflow",
  description: "Start a new innovation workflow project",
};

export default function NewLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
