export const metadata = {
  title: "Sign Up - Innovation Workflow",
  description: "Create a new account",
};

export default function SignupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
